using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCleaveHitbox : MonoBehaviour
{
	public int damage;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

	// update hitbox stats based on passed-in PlayerAttack
	public void UpdateStats(PlayerAttack playerAttack)
	{
		// depends on how the skill will change with upgrades
	}
}
